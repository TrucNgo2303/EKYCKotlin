<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Màu nền của nút -->
    <corners android:radius="8dp" /> <!-- Độ bo góc của nút, tùy chỉnh giá trị theo ý bạn -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" />
</shape>