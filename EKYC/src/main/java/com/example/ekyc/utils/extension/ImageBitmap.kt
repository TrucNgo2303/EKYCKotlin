package com.example.ekyc.utils.extension

import android.graphics.Bitmap
import androidx.appcompat.widget.AppCompatImageView

fun AppCompatImageView.setImageBitmap(bitmap: Bitmap?) {
    this.setImageBitmap(bitmap)
}